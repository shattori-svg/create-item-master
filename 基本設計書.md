# 商品登録取り込みファイル作成システム 基本設計書

**作成日**: 2026年3月6日  
**対象**: ロピアタイランド 情報システム部  
**バージョン**: 1.0  
**前提**: [要件定義書](./要件定義書.md) に基づく。

---

## 1. はじめに

### 1.1 目的

本ドキュメントは、要件定義書で確定した機能・仕様を**実装可能な形に落とし込む**ための基本設計とする。技術スタック、画面構成、データマッピング、バリデーション、外部連携方針を定義する。

### 1.2 参照ドキュメント

| ドキュメント | 用途 |
|--------------|------|
| [要件定義書](./要件定義書.md) | 機能要件・非機能要件の根拠 |
| [ヒアリング項目・要確認事項](./ヒアリング項目・要確認事項.md) | 回答済み仕様の一覧 |
| [docs/OIC_Tech_Spec_Item_Import_Template_Specification_20260116.md](./docs/OIC_Tech_Spec_Item_Import_Template_Specification_20260116.md) | インポートフォーマット詳細 |
| [docs/Item_260306_0906_sample.md](./docs/Item_260306_0906_sample.md) | 出力ファイル・Additional Barcode のサンプル |

---

## 2. システム構成

### 2.1 アーキテクチャ方針

- **クライアント**: Web ブラウザ（PC・タブレット）。単一画面（SPA）で完結。
- **サーバー**: 静的ファイル配信のみ想定（認証・DB なし）。オプションで軽量バックエンド（Node.js 等）を置く場合は、Excel 生成・スプレッドシート API 呼び出しをサーバー側で行う構成も可。
- **データの永続化**: なし。入力済みリストはメモリ（クライアント）のみ。復元はインポートファイル読み込みで対応。
- **外部連携**: Google スプレッドシート（マスタ参照）。書き込み機能は **Phase2 で後回し**。

### 2.2 構成パターン（推奨）

**パターンA: クライアント完結型（Phase1 推奨）**

- 静的 HTML / CSS / JavaScript（または React / Vue 等の SPA）。
- マスタデータ: 初回のみスプレッドシートから取得してキャッシュ、または静的 JSON に事前エクスポートして配布。
- Excel の読み書き: ブラウザ上で **SheetJS (xlsx)** 等のライブラリを使用。ファイル出力・インポートファイル読み込みはクライアントで完結。
- メリット: サーバー不要・デプロイが容易。URL を知っている人のみアクセス可能という要件に合致。

**パターンB: 軽量バックエンド併用型（スプレッドシート書き込み実装時）**

- フロントは SPA。バックエンドは Node.js 等で API を提供。
- マスタ取得・ItemList 書き込みはバックエンド経由で Google Sheets API を呼び出す（認証はサービスアカウント等）。
- Excel 生成をサーバー側で行うか、クライアントのままとするかは実装時に決定。

**Phase1 ではパターンA を推奨**。スプレッドシート書き込みは Phase2 でパターンB への拡張または別途 API 追加を検討する。

---

## 3. 技術選定

### 3.1 推奨技術スタック（Phase1）

| 区分 | 技術 | 備考 |
|------|------|------|
| フロントエンド | **HTML5 / CSS3 / JavaScript** または **React** / **Vue.js** | チームの習熟度・保守性で選択。SPA で単一画面。 |
| スタイル | CSS（BEM 等）または Tailwind CSS / 他 | PC・タブレット対応のレスポンシブ。 |
| Excel 操作 | **SheetJS (xlsx)** 等 | ブラウザで .xlsx の読み取り・生成。 |
| 多言語 | **JSON 辞書**（`ja.json`, `th.json`） | キーで切り替え、初期表示は日本語。 |
| マスタ取得 | Google Sheets API（公開範囲なら fetch）または **事前エクスポート JSON** | 認証なしで参照可能な場合は fetch。否则は運用で JSON 更新。 |
| バリデーション | クライアント側で実装 | 必須チェック、重複バーコード、文字数、GS-1 形式。 |
| デプロイ | 静的ホスティング（GitHub Pages / Netlify / 社内 Web サーバー） | HTTPS 推奨。 |

### 3.2 開発・ビルド

- バンドル: **Vite** または **Webpack**（React/Vue 利用時）。プレーン JS の場合はビルドなしでも可。
- テスト: 単体テスト（Vitest / Jest）、E2E（Playwright 等）は余力に応じて導入。

---

## 4. ディレクトリ構成（案）

```
create_item_import_system/
├── index.html                 # エントリ
├── src/
│   ├── main.js (or main.tsx)  # エントリスクリプト
│   ├── App.jsx                # ルートコンポーネント（SPA 時）
│   ├── components/            # 画面パーツ
│   │   ├── LanguageSwitcher.jsx
│   │   ├── DepartmentSelector.jsx
│   │   ├── ItemForm.jsx
│   │   ├── ItemList.jsx
│   │   ├── OutputSection.jsx  # シート選択・ファイル名・出力・インポート
│   │   └── ...
│   ├── lib/
│   │   ├── excel.js           # xlsx 読み書き・マッピング
│   │   ├── validation.js      # バリデーション
│   │   └── i18n.js            # 多言語切り替え
│   ├── data/
│   │   ├── departments.js     # 部門リスト（定数）
│   │   ├── masters.js         # マスタ取得結果キャッシュ or 初期値
│   │   └── constants.js       # 固定値・税率マップ等
│   └── locales/
│       ├── ja.json
│       └── th.json
├── public/
├── docs/                      # 仕様書 Markdown 等（既存）
├── 要件定義書.md
├── 基本設計書.md
└── package.json
```

---

## 5. 画面設計

### 5.1 画面構成

- **画面数**: **1 画面**（メイン画面のみ。遷移なし）。
- **レスポンシブ**: PC とタブレットを想定。ブレークポイントを設け、タブレットでは縦積みや折り返しで操作性を維持する。

### 5.2 メイン画面レイアウト（ワイヤーフレーム骨子）

- **レイアウト**: **入力フォーム**を左側、「**入力済み商品リスト**」を**その右側**に配置する（横並び）。下部に出力シート選択・ファイル名・ボタン群を置く。
- **部門**: **ドロップダウン**で単一選択（01–06）。
- **分類コード・仕入先**: **名称およびコードで検索可能なコンボボックス**。入力に応じて候補を絞り込み、選択する。
- **税率**: 初期値は **7%**。0%／7% のいずれかを選択可能。
- **商品リスト**: **表形式**（テーブル）で表示。列：チェック、バーコード、商品名(英)、商品名(泰)、分類、仕入先、原価、売価 等。行選択で編集、チェックで削除対象。
- **規格（英語）**: **初期値 1pcs**。規格（タイ）を追加。
- **販売入数**: **初期値 1**。**2 以上のとき**「ケースJANコード」「ケース売価」の入力欄を表示する。
- **発注リードタイム**・**ブランド（英語・タイ語の各入力ボックス）**を項目として追加。

```
+----------------------------------------------------------------------------------------------------------+
| [日本語] [ไทย]                                                                                             |
+----------------------------------------------------------------------------------------------------------+
| 部門: [▼ 01 食品部門      ]                                                                               |
+----------------------------------------------------------------------------------------------------------+
| 入力フォーム（左）                    | 入力済み商品リスト（右・表形式） 最大100件                          |
+--------------------------------------+-------------------------------------------------------------------+
| 分類コード: [🔍 検索/選択      ▼]   | | ☐ | バーコード    | 商品名(英)   | 分類     | 原価  | 売価 | … | 操作 |
|   （名称・コードで検索、AI候補表示）  | |---|---------------|------------|---------|------|-----|-----|------|
| バーコード: [________________]       | | ☐ | 4901111275195 | MAXIM ...  | 110113… | 258  | 399 | … | 編集 |
| 商品名(英): [________________] 60字 | | ☐ | 4901940039333 | Amizyaga.. | 110301… | 39.9 | 59  | … | 編集 |
| 商品名(泰): [________________] 60字 | | … |               |            |         |      |     |     |      |
| 規格(英):   [1pcs        ]  ←初期値1pcs | [削除]                                                                 |
| 規格(泰):   [________________]       | 出力シート: ☑ Item  ☑ Additional Barcode                              |
| 税率:       [▼ 7% ]  ←初期値7%      | ファイル名: [________________________________] [ファイル出力] [読込]   |
| 仕入先:     [🔍 検索/選択      ▼]   | (Spread書き込みは Phase2)                                                |
| 原価: [____]  発注入数: [__]         +-------------------------------------------------------------------+
| 発注リードタイム: [__] 日             |
| 売価: [____]  販売入数: [1 ]  ←初期値1 |
|  （販売入数2以上で表示）             |
| ケースJANコード: [____________]      |
| ケース売価:     [____]              |
| ブランド(英): [________________]   |
| ブランド(泰): [________________]   |
| [入力完了]                          |
+--------------------------------------+
```

### 5.3 コンポーネント責務（案）

| コンポーネント | 責務 |
|----------------|------|
| LanguageSwitcher | 言語切替（ja/th）。初期は日本語。 |
| DepartmentSelector | 部門の**ドロップダウン**で単一選択（01–06）。リストに商品がある間は変更不可（警告表示）。 |
| ItemForm | 必須項目の入力。**分類・仕入先はコンボボックス**。**規格（英）初期値 1pcs**、**規格（タイ）**追加。税率は初期値 7%。**販売入数は初期値 1**。販売入数 2 以上で**ケースJANコード・ケース売価**欄を表示。**発注リードタイム**・**ブランド（英語・タイ語の各ボックス）**を追加。バリデーション表示（枠色・メッセージ）。 |
| ClassificationCombo | 分類マスタ（group）を参照。**名称（Description 等）および Product Group Code で検索**。**商品名から AI で推測した候補**を表示し、ユーザーが選択または検索で変更可能。 |
| SupplierCombo | 仕入先マスタ（supplier）を参照。**名称（Abbreviation 等）および Supplier No. で検索**可能なコンボボックス。 |
| ItemListTable | 入力済み一覧を**表形式**で表示。列：チェック、バーコード、商品名(英)、商品名(泰)、分類、仕入先、原価、売価 等。行選択で編集、チェックで削除対象。最大100件。 |
| OutputSection | 出力シートチェック、ファイル名入力、ファイル出力ボタン、インポートファイル読み込みボタン。 |
| ErrorDisplay | 必須未入力時はポップアップではなく、該当フィールドの枠色変更＋メッセージ表示。 |

### 5.4 分類コードの AI 推測

- **概要**: 商品名（英語・タイ語のいずれかまたは両方）から、**分類コード（Product Group Code）を AI で推測**し、候補として表示する。
- **挙動**: 商品名を入力したタイミング（または「推測」ボタン押下）で、入力テキストに基づき分類候補を取得する。候補はコンボボックスの上位に表示し、ユーザーはそのまま選択するか、検索で別の分類を選べる。
- **実装方針**: 軽量な場合はキーワード・マスタの Description とのマッチングで候補表示。外部 API（LLM 等）を利用する場合は、プライバシー・コスト・レスポンスを考慮して選定する。詳細は詳細設計・開発時に決定する。

### 5.5 エラー表示仕様

- **必須未入力**: 該当入力ボックスを赤枠等でハイライトし、直下または横にエラーメッセージを表示（日本語・タイ語の両方）。ポップアップは使用しない。
- **バーコード重複**: 「入力完了」時にリスト内に同一バーコードがあれば追加せず、バーコード欄付近にエラーメッセージを表示。
- **ファイル出力前バリデーション**: エラーがある場合は出力しない。画面上部または出力セクション付近にエラー内容を一覧表示する。

---

## 6. データ定義・マッピング

### 6.1 フォーム項目と Item シートのマッピング

| フォーム項目 | Item シート列名 | 備考 |
|--------------|-----------------|------|
| 部門（リスト共通） | Department Code | 01–06 |
| 分類コード | Product Group Code | マスタ group の Product Group Code |
| バーコード | Barcode No. | GS-1 形式 |
| （なし） | Item No. | **空欄固定**（基幹で自動採番） |
| 商品名（英語） | Description (ENG) | 最大60文字 |
| 商品名（タイ語） | Description (THA) | 最大60文字 |
| （なし） | Description (JPN) | 空欄可 |
| 商品名（英語） | Barcode Description (ENG) | 同一値でコピー |
| 商品名（タイ語） | Barcode Description (THA) | 同一値でコピー |
| 規格（英語） | Size Specification (ENG) | **初期値 1pcs** |
| 規格（タイ） | Size Specification (THA) | |
| （なし） | Size Specification (JPN) | 空欄可 |
| 税率 | VAT Prod. Posting Group | **初期値 7%**。7% → VAT07、0% → 0%用区分（仕様書で確認） |
| 仕入先コード | Vendor No. (default) | マスタ supplier の Supplier No. |
| 原価 | Unit Cost (default) | |
| 発注入数 | （Item にはない。Additional Barcode 生成に使用） | |
| 発注リードタイム | Lead Time Calculation (default) | 日数等 |
| 売価 | Unit Price (default) | |
| 販売入数 | （Additional Barcode 生成に使用） | **初期値 1**。2以上でケースJAN・ケース売価入力欄表示 |
| ケースJANコード | Additional Barcode の Barcode 列（該当行） | 販売入数2以上時のみ入力。ケース販売用バーコード |
| ケース売価 | Additional Barcode 販売入数行の Unit Price | 販売入数2以上時のみ入力 |
| ブランド（英語） | Brand (ENG) | 英語用入力ボックス |
| ブランド（タイ） | Brand (THA) | タイ語用入力ボックス |

**固定値で埋める列（要件定義書 6.1 準拠）**: Barcode Type=Manufacturer, Base Unit of Measure=PCS, Inventory Type=Inventory, Inventory Posting Group=TRADE, Gen. Prod. Posting Group=TRADE, Keying in Price=Not Mandatory, Keying in Quantity=Not Mandatory, Zero Price Valid=0, No Discount Allowed=0, Blocked=0, Auto-Replenishment=0, Weight Price=0, Height/Width/Length/Weight=0。その他は仕様書・サンプルに合わせる。

### 6.2 Additional Barcode シートの自動生成ルール

**基本**: 1 商品あたり **発注入数** に対応する 1 行を出力する。

**販売入数が 2 以上かつ 販売入数 ≠ 発注入数 の場合**: **行を 1 行追加**し、計 2 行とする。  
- **1 行目（発注入数の行）**: Default Purchase UOM = **1**、Barcode Unit of Measure = "CTN" + 発注入数  
- **2 行目（販売入数の行）**: Default Purchase UOM = **0**、**Barcode Unit of Measure（UOM）= 「Pack」**

共通・行別の算出は以下のとおり。

| Additional Barcode 列 | 発注入数の行 | 販売入数の行（販売入数≥2 かつ ≠発注入数 のときのみ追加） |
|------------------------|--------------|----------------------------------------------------------|
| Barcode No. | **ケースJANコード**が入力されていればその値を入れる。なければ空欄 | 販売入数行では空欄（または仕様書に従う） |
| Base Barcode No. | フォームの「バーコード」 | フォームの「バーコード」 |
| Barcode Description (ENG)/(THA)/(JPN) | 商品名・規格をコピー | 同左 |
| Barcode Unit of Measure | **"CTN" + 発注入数**（例: CTN12） | **「Pack」**（販売入数2以上かつ発注入数と異なる場合の UOM） |
| Qty per Unit of Measure | 発注入数 | 販売入数 |
| Default Purchase UOM | **1** | **0** |
| Size Specification (ENG)/(THA)/(JPN) | 規格（英）・規格（タイ）をコピー | 同左 |
| Height, Width, Length, Weight | 0 | 0 |
| Unit Cost | **原価 × 発注入数**（ケース単価） | 単品原価等（仕様書で確認） |
| Unit Price | 0 またはケース売価（仕様書で確認） | **ケース売価**の値を入れる |

※ 販売入数の行は「販売入数が 2 以上かつ 発注入数と異なる」場合にのみ追加。そのときケースJANコード・ケース売価はフォームで入力された値を使用する。正式な算出ルールは基幹仕様書で確認すること。

### 6.3 インポートファイル読み込み時のマッピング

- アップロードした .xlsx の **Item** シートを解析する。
- 列名（1 行目）が「Department Code」「Product Group Code」「Barcode No.」「Description (ENG)」等と一致する列を読み、リスト用の内部データに変換する。
- 既存リストへの「追記」か「置き換え」は UI で選択可能とする。追記時は**バーコード重複**している行はスキップするかエラー表示する。
- Additional Barcode シートは読み込み対象に含めてもよいが、リスト復元の主目的は Item の再編集なので、**Item シートのみ読み込む**実装でも可。仕様書で定義する。

---

## 7. バリデーション一覧

| 項目 | ルール | エラー時表示 |
|------|--------|--------------|
| 必須項目 | すべて入力されていること | 未入力項目を枠色付け＋メッセージ |
| バーコード | GS-1 形式（桁数・チェックディジット） | 該当欄にメッセージ |
| バーコード重複 | リスト内に同一バーコードが存在しないこと | 追加せず、バーコード欄にメッセージ |
| 商品名(英)(泰) | 各 60 文字以内 | 超過時は枠色付け＋メッセージ |
| 発注入数・販売入数 | 数値のみ。販売入数は初期値 1 | 不正時は枠色付け＋メッセージ |
| 販売入数 2 以上時 | ケースJANコード・ケース売価を入力すること（必須扱いとするかは実装時決定） | 未入力時は警告または出力時エラー |
| リスト件数 | 最大 100 件 | 超過時は追加せず警告表示 |
| 部門変更 | リストが空のときのみ変更可 | 変更しようとしたら警告 |

**ファイル出力前**: 上記に加え、出力対象データの整合性をチェック。エラーがあれば**出力しない**し、画面上にエラー内容を表示する。

---

## 8. 多言語対応

- **初期表示**: 日本語。
- **切り替え**: 画面上部の「日本語」「ไทย」等のボタンで切り替え。切り替え後はラベル・ボタン・エラーメッセージをすべて選択中言語で表示する。
- **辞書**: `locales/ja.json`, `locales/th.json` を用意し、キー（例: `form.barcode`, `error.required`）で参照する。
- **入力値**: ユーザーが入力する「商品名」等は言語に依存しない。表示ラベルのみ言語切り替えの対象とする。

---

## 9. 外部連携（Google スプレッドシート）

### 9.1 Phase1（本番スコープ）

- **マスタ参照**: 分類マスタ（シート名 **group**）、仕入先マスタ（シート名 **supplier**）を参照する。
  - スプレッドシートが「リンクを知っている全員が閲覧可」であれば、公開 URL 経由で CSV または JSON として取得する方法がある。
  - 認証が必要な場合は、**事前にマスタをエクスポートした JSON をアプリに同梱**し、定期更新は手動または別ジョブで行う運用でも可。
- **書き込み**: **Phase2 で後回し**。ItemList シートへの追記は実装しない。

### 9.2 Phase2（スプレッドシート書き込み実装時）

- Google Sheets API を利用。認証は**サービスアカウント**または**OAuth**のいずれかを選定する。
- スプレッドシート ID: `1YpnPVR4_7VboHqFVP5Ap-V8s8W32TUhsvtaSECyrnBw`
- 書き込み先: シート「**ItemList**」。既存シートの**末尾に追記**する。

---

## 10. ファイル出力・ファイル名

- **形式**: .xlsx。拡張子は**アプリで付与**する。
- **デフォルトファイル名**: `{部門コード}_{YYYYMMDD_HHMMSS}.xlsx`（例: `01_20260306_143052.xlsx`）。ユーザーがファイル名を入力している場合はその値 + `.xlsx` とする。
- **出力シート**: ユーザーがチェックしたシートのみ出力。デフォルトは「Item」「Additional Barcode」にチェック。

---

## 11. 今後の詳細化・実装時の確認事項

- 0% 税率の基幹側コード（VAT00 等）の正式名称。
- Additional Barcode の Unit Cost が「原価×発注入数」でよいか、端数処理の有無。
- インポートファイル読み込みで「Item のみ」か「Item + Additional Barcode の両方」を読むか。
- マスタ（group / supplier）の取得方法の最終方針（公開スプレッドシート vs 事前エクスポート JSON）。

---

**以上、基本設計書とする。実装は本設計書および要件定義書に従う。**
