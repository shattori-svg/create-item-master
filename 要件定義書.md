# 商品登録取り込みファイル作成システム 要件定義書

**作成日**: 2026年3月6日  
**対象**: ロピアタイランド 情報システム部  
**バージョン**: 1.0

---

## 1. はじめに

### 1.1 背景・目的

基幹システムの業務改善のため、商品登録用の取り込みファイルを効率的に作成するシステムを開発する。  
項目が多岐にわたり難易度が高いため、現場への引き渡しが進んでいない現状を解消し、**グロサリー部門のメーカーバーコード商品**に限定したツールを先行して提供する。

### 1.2 対象範囲

| 項目 | 内容 |
|------|------|
| 対象部門 | グロサリー部門 |
| 対象商品 | メーカーバーコード商品のみ |
| 対象外（将来対応） | 生鮮部門の原料発注など複雑なケース |

### 1.3 部門リスト

入力フォームの「部門」で選択する部門コードと名称は以下のとおりとする。

| 部門コード | 部門名 |
|------------|--------|
| 01 | 食品部門 |
| 02 | 青果部門 |
| 03 | 鮮魚部門 |
| 04 | 精肉部門 |
| 05 | 総菜部門 |
| 06 | 店舗管理部門 |

※ 本システムの対象は**グロサリー（食品部門）**のため、フォームでは上記リストから選択するが、現時点では部門コード **01** を前提とした運用とする。

### 1.4 用語・参照ドキュメント

| 用語 | 説明 |
|------|------|
| 基幹システム | 商品マスタ等を管理する既存システム。本システムが出力したファイルをインポートする。 |
| インポートフォーマット | 基幹システムが定める取り込みファイルの仕様。本プロジェクト直下の「OIC Tech Spec Item Import Template Specification 20260116」を参照する。 |
| サンプルファイル | 実際のインポートファイル例。「Item_260306 0906 (1)」を参照する。 |

---

## 2. システム概要

### 2.1 アプリ種別・技術方針

| 項目 | 要件 |
|------|------|
| アプリ種別 | **Webアプリケーション** |
| データベース | **使用しない**（ファイル出力・Googleスプレッドシート連携で完結） |
| 認証 | **不要**。**アクセスはURLを知っている人が全員アクセス可能**（ログインなし）。**理由**: 取り扱うデータに機密性がそこまで高くないこと、データベースを用いていない（永続保存はファイル出力・スプレッドシート連携に限定）ことから、認証を設けない方針とする。 |
| アーキテクチャ | 要件に基づき最適な方式を選定する。候補例: 静的HTML/JS + クライアントサイド処理（マスタ・出力はGoogleスプレッドシートAPI連携）、または軽量バックエンド（Node.js等）でスプレッドシート連携・ファイル生成を担う方式。DB不要・認証不要のため、SPA（Single Page Application）構成が適している。 |

### 2.2 対応言語

- **日本語**
- **タイ語**

**初期表示言語は日本語**とする。画面内で言語を切り替え可能とし、表示文言を両言語で提供する。

### 2.3 Googleスプレッドシート連携

- マスタデータ（分類マスタ、仕入先マスタなど）の参照元としてGoogleスプレッドシートを使用する。
- 出力データをGoogleスプレッドシートへ書き込む機能を提供する。

**連携先スプレッドシート（lopia thailand item import master）**

| 項目 | 内容 |
|------|------|
| スプレッドシート名 | lopia thailand item import master |
| URL | [https://docs.google.com/spreadsheets/d/1YpnPVR4_7VboHqFVP5Ap-V8s8W32TUhsvtaSECyrnBw/edit](https://docs.google.com/spreadsheets/d/1YpnPVR4_7VboHqFVP5Ap-V8s8W32TUhsvtaSECyrnBw/edit) |
| スプレッドシートID | `1YpnPVR4_7VboHqFVP5Ap-V8s8W32TUhsvtaSECyrnBw` |

**マスタシート名・列構成**

| マスタ | シート名 | 列（ヘッダー） | フォームでの利用 |
|--------|----------|----------------|------------------|
| 分類マスタ | **group** | Item Category Code, Product Group Code, Description, Description (THA), Description (JPN) | 分類コードのドロップダウン：**Product Group Code** を値、Description（または Description (THA)）を表示用に使用 |
| 仕入先マスタ | **supplier** | Departent, Delivery to, Trade, Supplier No., Abbreviation, Supplier Official Name (English）, Supplier Official Name (Thai） | 仕入先コードのドロップダウン：**Supplier No.** を値、Abbreviation または公式名称を表示用に使用 |

**Spreadシート書き込み**

| 項目 | 内容 |
|------|------|
| 書き込み先シート | **ItemList**（上記ブック内の固定シート） |
| 書き込み方法 | **既存シートの末尾に追記** |
| **実装優先度** | **後回し**（Phase2 または別リリース）。まずはファイル出力・インポートファイル読み込み等を先行して実装する。 |

**アクセス・権限**

- 本アプリへのアクセスは**URLを知っている人が全員アクセス可能**とする（認証・ログインは行わない）。スプレッドシートの参照・書き込みを行う場合、実装時には利用者側の権限（例: リンクを知っている人に共有されたスプレッドシート）またはサービス側の認証（OAuth／サービスアカウント等）のいずれかで対応する。

※ Departent は Department の表記ゆれの可能性あり。

---

## 3. 非機能要件（UI/UX）

- **利用想定デバイス**: **PC および タブレット**を想定する。レイアウト・操作性は両方で使いやすいようにする。
- PC操作に不慣れな利用者でも使えるよう、**極力直感的**で、**初めての人でも理解できる**デザインとする。
- **親切で分かりやすいUI/UX**を最優先とし、ラベル・説明・エラーメッセージを明確にする。
- **必須未入力時**: ポップアップは使わず、**該当入力ボックスに色付け**（例: 枠や背景を赤系でハイライト）し、**その近くまたはボックス下にエラーメッセージを表示**して入力を促す。メッセージは日本語・タイ語の両方で表示する。
- 言語切り替えにより、日本語・タイ語のいずれでも同じ操作性を維持する。

---

## 4. 画面設計

### 4.1 メイン画面レイアウト

メイン画面は1画面で完結し、以下の要素で構成する。

| 位置 | 要素 | 説明 |
|------|------|------|
| **画面左上部** | 言語切り替えボタン | 日本語／タイ語の切り替え |
| **画面上部** | **部門（単一選択）** | **ドロップダウン**で部門を1つのみ選択（01–06）。1回のファイル生成では1部門に限定。選択した部門がリストに追加する全商品に適用される。リストに商品がある間は変更不可（変更時は警告）。 |
| **画面左側** | 入力フォーム部 | **入力フォーム枠の左側**に配置。部門以外の項目（分類コード、バーコード、商品名 等）を入力。**分類コード・仕入先は名称およびコードで検索可能なコンボボックス**。**分類コードは商品名から AI で推測した候補**を表示可能とする。**税率の初期値は 7%**。 |
| **画面右側（フォームの右隣）** | 入力済み商品リスト | **入力フォーム枠の右側**に配置。**表形式**で表示。入力完了で追加された商品の一覧。行選択で編集、チェックで削除対象。同一部門の商品のみ。**最大件数は約 100 件**。超過時は警告または追加制限。 |
| **画面右下部** | 出力シート選択チェックボックス | 出力対象シート（Item, Additional Barcode 等）を選択。デフォルトは「Item」「Additional Barcode」にチェック |
| **画面右下部** | 出力ファイル名入力ボックス | 出力ファイル名を指定。未入力時はデフォルト命名則を適用（選択中の部門コード＋タイムスタンプ） |
| **画面右下部** | ファイル出力ボタン | 選択シートに従いインポート用ファイルをダウンロード（**単一部門の商品のみ**が出力される） |
| **画面右下部** | Spreadシート書き込みボタン | 入力済み商品データを上記ブックのシート「ItemList」の末尾に追記する |
| **画面右側 または 左側** | インポートファイル読み込み | インポート形式の .xlsx ファイルを選択し、**入力済みリストに復元**する。本アプリ出力ファイルを読み込むことでリストを再現できる。 |

※ 右下部のボタン・入力欄は配置・グループ化を整理し、誤操作を防ぐレイアウトとする。  
※ **一回のファイル生成＝単一部門**とするため、部門は必ず単一選択のUIとし、複数部門が混在しないようにする。

---

## 5. 業務フロー

1. **アクセス**  
   ユーザーは認証（ログイン）なしでアプリにアクセスする。

2. **部門の選択**  
   画面上部の**部門**を**ドロップダウン**で**1つだけ選択**する。この部門が、このあと追加する全商品および出力ファイルに適用される。リストに商品が1件でもある場合は部門の変更は行わず、変更したい場合はいったんリストを空にしてから部門を選び直す。

3. **入力**  
   入力フォームで部門以外の必要項目（分類コード、バーコード、商品名 等）を入力する（Googleフォームのような形式）。

4. **リスト追加**  
   「入力完了」ボタン押下で、入力内容に**選択中の部門**を付与して**入力済み商品リスト**に1件追加される。

5. **編集**  
   入力済み商品リストで商品を選択すると、その内容をフォーム上で編集できる（部門はリスト共通のため変更不可）。

6. **削除**  
   入力済み商品リストでチェックボックスにより商品を選択し、「削除」ボタン押下でリストから削除する。

7. **出力シート選択**  
   出力したいシートをチェックボックスで選択する。  
   **デフォルト**: 「Item」「Additional Barcode」にチェックが入っている。

8. **ファイル出力**  
   「ファイル出力」ボタン押下で、出力前のバリデーションを実行する。**バリデーションエラーがある場合は出力しない**（ダウンロードを行わず、エラー表示で修正を促す）。エラーがない場合のみ、選択したシートに従いインポートフォーマットに則ったファイルを出力（ダウンロード）する。リスト内の商品はすべて同一部門のため、**1ファイルに単一部門のデータのみ**が含まれる。

9. **スプレッドシート書き込み**（任意・**後回し**）  
   「Spreadシート書き込み」ボタン押下で、入力済み商品データを上記ブックのシート「ItemList」の末尾に追記する。本機能は**実装を後回し**とし、まずファイル出力・インポート読み込み等を優先する。

10. **インポートファイルの読み込み（リスト復元）**  
    本アプリが出力したインポート形式のファイル（.xlsx）を読み込むことで、**入力済み商品リストに復元**できる機能を提供する。ユーザーがファイルを選択（アップロード）すると、Item シート等の内容を解析し、リストに商品として追加する。既存リストに追記するか、リストを置き換えるかは実装時に UI で選択可能とする。これにより、ブラウザを閉じた後も、保存したインポートファイルからリストを再現できる。

---

## 6. 入力仕様

### 6.1 入力必須項目

以下の項目はすべて**必須**とする。

| No. | 項目名 | 説明 |
|-----|--------|------|
| 1 | 部門 | 部門コード（01–06）。**ドロップダウン**でリスト単位に単一選択。選択肢は「1.3 部門リスト」を参照。選択中の部門が全商品に適用され、商品ごとの部門入力は行わない。 |
| 2 | 分類コード | 商品分類。**名称およびコードで検索可能なコンボボックス**で分類マスタを参照。**商品名から AI で推測した候補**を表示し、ユーザーが選択または検索で変更可能。 |
| 3 | バーコード | メーカーバーコード。**GS-1 に則る形式**（桁数・チェックディジット等は GS-1 規格に準拠）。 |
| 4 | 商品名（英語） | 英語名称。**入力可能文字数は 60 文字まで**。 |
| 5 | 商品名（タイ語） | タイ語名称。**入力可能文字数は 60 文字まで**。 |
| 6 | 規格（英語） | 規格表記（英語）。出力時は **Size Specification (ENG)** にマッピング。**初期値は 1pcs**。 |
| 6a | 規格（タイ） | 規格表記（タイ語）。出力時は **Size Specification (THA)** にマッピング。 |
| 7 | 税率 | **0% または 7% のいずれか**。ドロップダウン等で2択から単一選択。**初期値は 7%**。出力時は基幹の税率区分（例: VAT07＝7%、0%用の区分）にマッピングする。 |
| 8 | 仕入先コード | **名称およびコードで検索可能なコンボボックス**で仕入先マスタを参照。 |
| 9 | 原価 | 仕入原価 |
| 10 | 発注入数 | 発注単位の入数。**数値のみ**で入力（例: 12）。単位付き（CTN12 等）は入力しない。 |
| 10a | 発注リードタイム | 発注リードタイム（日数等）。基幹の Lead Time Calculation にマッピング。 |
| 11 | 売価 | 販売単価 |
| 12 | 販売入数 | 販売単位の入数。**数値のみ**で入力。**初期値は 1**。単位付きは入力しない。**2 以上のとき**は「ケースJANコード」「ケース売価」の入力欄を表示し、入力させる。 |
| 12a | ケースJANコード | **販売入数が 2 以上のときのみ表示**。ケース販売用バーコード。Additional Barcode の Barcode 列等に使用。 |
| 12b | ケース売価 | **販売入数が 2 以上のときのみ表示**。ケース販売時の売価。Additional Barcode の販売入数行の Unit Price に使用。 |
| 13 | ブランド（英語） | ブランド名（英語）。出力時は Brand (ENG) にマッピング。 |
| 13a | ブランド（タイ） | ブランド名（タイ語）。出力時は Brand (THA) にマッピング。 |

**規格・販売入数の初期値**

- **規格（英語）**: **初期値 1pcs**。ユーザーが変更可能。
- **販売入数**: **初期値 1**。**販売入数が 2 以上のとき**は、ケースJANコード・ケース売価の入力が求められ、出力時バリデーションでチェックする（未入力時はエラー表示または警告）。

**規格（タイ）・発注リードタイム・ブランド**

- **規格（タイ）**・**発注リードタイム**・**ブランド（英語）**・**ブランド（タイ）**は入力項目として追加。ブランドは**英語用・タイ語用の入力ボックスをそれぞれ用意**する。必須とするか任意とするかは実装時に決定可能。

**Item No.（商品番号）について**

- **Item No. は空欄固定**とする。本アプリではフォームに項目を設けず、出力ファイルの Item No. 列も空欄で出力する。
- 基幹システムは Item No. が空欄の取り込みを「新規作成」として扱い、自動採番する。

**出力時に固定値で埋める項目**

インポートフォーマットのうち、フォームで入力しない項目は**アプリ側で固定値**を設定して出力する。主な固定値は以下のとおり（サンプル「Item_260306 0906 (1)」および仕様書に準拠）。詳細は開発時に仕様書で確認すること。

| 項目（例） | 固定値（例） | 備考 |
|------------|--------------|------|
| Barcode Type | Manufacturer | メーカーバーコード商品のため |
| Base Unit of Measure | PCS | 本システム対象の前提 |
| Inventory Type | Inventory | |
| Inventory Posting Group | TRADE | |
| Gen. Prod. Posting Group | TRADE | |
| VAT Prod. Posting Group | VAT07 または 0%用区分 | フォームの「税率」0%／7%に応じてマッピング |
| Keying in Price | Not Mandatory | |
| Keying in Quantity | Not Mandatory | |
| Zero Price Valid | 0 | |
| No Discount Allowed | 0 | |
| Blocked | 0 | |
| Auto-Replenishment | 0 | |
| Weight Price | 0 | |
| Height, Width, Length, Weight | 0 | 未入力時など |

**入力制約（文字数・形式）**

- **商品名（英語）・商品名（タイ語）**: それぞれ**60 文字まで**入力可能。超過時はバリデーションでエラーとする。
- **バーコード**: **GS-1 に則る形式**とする。実装時は GS-1 の桁数・チェックディジット等に従いバリデーションする。

**必須未入力時の表示**

- 必須項目が未入力のまま「入力完了」した場合、**追加は行わず**、**ポップアップではなく**次のように表示する。**該当入力ボックスに色付け**（枠・背景のハイライト）し、**ボックス付近にエラーメッセージ**を表示して入力を促す。エラーメッセージは日本語・タイ語の両方で表示する。

**重複登録の防止**

- **同じバーコードの重複登録はアプリ側でできないようにする**。入力済み商品リストに既に同じバーコードが含まれる場合、「入力完了」時には追加を許可せず、エラー表示する。インポートファイル読み込み時も、重複するバーコードはスキップするかエラーとするなど、リスト内でバーコードが一意になるようにする。

※ 上記は要件ベース。実際の項目名・桁数・形式は「OIC Tech Spec Item Import Template Specification 20260116」および「Item_260306 0906 (1)」に合わせて確定する。

### 6.2 マスタ参照（コンボボックス）

フォームでは**名称およびコードで検索可能なコンボボックス**で以下を参照する。**データソースは上記スプレッドシート「lopia thailand item import master」の指定シート**とする。

| マスタ | シート名 | 値として使う列 | 表示・検索に使う列 | 備考 |
|--------|----------|----------------|---------------------|------|
| 分類マスタ | **group** | Product Group Code | Description, Description (THA), Description (JPN), Product Group Code | **商品名から AI で分類を推測**し、候補を表示。ユーザーは候補を選択するか、名称・コードで検索して選択。 |
| 仕入先マスタ | **supplier** | Supplier No. | Abbreviation, Supplier Official Name (English / Thai）, Supplier No. | 名称またはコードで検索して選択。 |

**分類マスタ（group）の列**: Item Category Code, Product Group Code, Description, Description (THA), Description (JPN)  
**仕入先マスタ（supplier）の列**: Departent, Delivery to, Trade, Supplier No., Abbreviation, Supplier Official Name (English）, Supplier Official Name (Thai）

---

## 7. 出力仕様

### 7.1 出力形式

- **出力ファイル形式は Excel（.xlsx）のまま**とする。CSV 等の別形式は対象外。
- **インポートフォーマット**に完全に準拠し、出力ファイルを**そのまま基幹システムに取り込み可能**な形式で整形する。
- **バリデーションエラー時は出力しない**。出力前にバリデーションを実行し、エラーがある場合はファイル出力（ダウンロード）を行わず、エラー内容を画面で表示して修正を促す。
- フォーマットの詳細は、プロジェクト直下の  
  **「OIC Tech Spec Item Import Template Specification 20260116」**  
  を参照する。

### 7.2 出力対象シート

商品登録で主に利用するのは、Item Import Export シートの以下とする。

- **Item**
- **Additional Barcode**（AdditionalBarcode）

ユーザーはチェックボックスで出力するシートを選択する。デフォルトは「Item」「Additional Barcode」にチェックを入れた状態とする。

**Additional Barcode の登録単位・入力方法**

- **基本**: 1 商品あたり Additional Barcode は **発注入数** に対応する 1 行を出力する（ケース単位）。
- **販売入数が 2 以上かつ 販売入数 ≠ 発注入数 の場合**: **行を 1 行追加**する。  
  - **発注入数の行**: Default Purchase UOM = **1**。  
  - **販売入数の行**（追加行）: Default Purchase UOM = **0**。**Barcode Unit of Measure（UOM）は「Pack」**とする。  
  その場合、発注入数行が「1」、販売入数行が「0」となる。
- **Additional Barcode の Barcode 列**: ケース販売用バーコード（**ケースJANコード**）が入力されていたら、その値を入れる。入力がなければ空欄等、仕様書に合わせる。
- **販売入数の行**（上記の追加行）の **Unit Price**: **ケース売価**の値を入れる。
- ボール販売商品は本ルールの対象外（別扱い）。本システムの対象はグロサリー部門メーカーバーコード商品。
- **Additional Barcode の内容は原則として自動生成**。ケースJAN・ケース売価はフォームで販売入数 2 以上時にユーザーが入力する。詳細な算出ルールは基本設計で定義する。

### 7.3 ファイル名

- **デフォルト**: `{部門コード(01–06)}_{タイムスタンプ}`  
  **タイムスタンプ形式**: `YYYYMMDD_HHMMSS`（例: 20260306_143052）。  
  例: `01_20260306_143052`
- **任意指定**: 画面右下部の「出力ファイル名入力ボックス」で変更可能とする。
- **拡張子**: **アプリで付与**する。ユーザーが拡張子を入力しなくても、出力時に `.xlsx` を付与する。

### 7.4 サンプルファイル

実際のインポートファイルの体裁は、プロジェクト直下の  
**「Item_260306 0906 (1)」**  
をサンプルとして参照する。

---

## 8. 機能一覧（サマリ）

| No. | 機能 | 概要 |
|-----|------|------|
| 1 | 言語切り替え | 日本語／タイ語の切り替え（画面左上部） |
| 2 | 部門の単一選択 | **ドロップダウン**でリスト単位に部門を1つだけ選択。1回のファイル生成＝単一部門。リストに商品がある間は部門変更不可。 |
| 3 | フォーム入力 | 部門以外の必須項目の入力。**規格（英語）初期値 1pcs**、**規格（タイ）**追加。分類・仕入先は**コンボボックス**。**税率初期値 7%**。**販売入数初期値 1**。**販売入数 2 以上**のとき**ケースJANコード・ケース売価**の入力欄を表示。**発注リードタイム**・**ブランド（英語・タイ語の各ボックス）**を追加。 |
| 3a | 分類コードの AI 推測 | 商品名（英語・タイ語）から**分類コードを AI で推測**し、候補を表示。ユーザーは候補を選択するか、検索で任意の分類を選択可能。 |
| 4 | 入力完了 | 選択中の部門を付与して入力内容を入力済み商品リストに追加 |
| 5 | リスト編集 | リストで選択した商品の内容をフォームで編集（部門は変更不可） |
| 6 | リスト削除 | チェックした商品をリストから削除 |
| 7 | 出力シート選択 | Item / Additional Barcode 等のチェックボックス（デフォルトは Item, Additional Barcode） |
| 8 | ファイル名指定 | 出力ファイル名の入力（任意。未入力時は選択中の部門コード＋タイムスタンプ） |
| 9 | ファイル出力 | 単一部門のデータのみを含むインポート用ファイルのダウンロード |
| 10 | スプレッドシート書き込み | 入力済みデータのGoogleスプレッドシートへの書き込み（**後回し**。Phase2 で実装） |
| 11 | インポートファイル読み込み（リスト復元） | インポート形式の .xlsx を読み込み、入力済み商品リストに復元する。本アプリ出力ファイルからリストを再現可能。 |

---

## 9. 制約・前提

| 項目 | 内容 |
|------|------|
| DB | 使用しない。セッション・一覧データはクライアント側（または一時ファイル）で保持する想定。 |
| 認証 | 行わない。**アクセスはURLを知っている人が全員アクセス可能**。データの機密性がそこまで高くなく、DB を用いていないため認証なしとする。 |
| 対象商品 | グロサリー部門のメーカーバーコード商品に限定。 |
| フォーマット | 基幹システムの「OIC Tech Spec Item Import Template Specification 20260116」およびサンプル「Item_260306 0906 (1)」に準拠。 |

---

## 10. 今後の検討事項（メモ）

- 生鮮部門・原料発注など、他部門・複雑ケースの対象化
- マスタの更新頻度とスプレッドシート連携の運用方法
- 出力ファイルのバリデーション（必須項目・形式チェック）の詳細

---

## 11. 要確認・ヒアリング項目

本仕様書で**まだ詰め切れていない箇所**は、別ドキュメントにヒアリング用の質問として整理しています。  
キックオフや関係者とのすり合わせ時に参照し、回答を反映したうえで仕様を確定してください。

→ **[ヒアリング項目・要確認事項.md](ヒアリング項目・要確認事項.md)**

---

## 付録A. インポートフォーマット・仕様の参照先

### 元ファイル（Excel）

- **仕様書（必須）**: プロジェクト直下  
  `OIC Tech Spec Item Import Template Specification 20260116.xlsx`  
  → Item / AdditionalBarcode シートの列定義・データ型・必須有無は本ファイルに従う。
- **サンプル（体裁確認用）**: プロジェクト直下  
  `Item_260306 0906 (1).xlsx`  
  → 実際のインポートファイルの見本。出力ファイルはこの体裁に合わせて整形する。

### 閲覧用 Markdown（Excel を参照しやすくしたもの）

Excel はバイナリのため、**同じ内容を Markdown に変換したファイル** を `docs/` に用意している。エディタや GitHub で検索・閲覧する際はこちらを利用するとよい。

| 用途 | 参照先 |
|------|--------|
| 仕様書の内容を閲覧 | [`docs/OIC_Tech_Spec_Item_Import_Template_Specification_20260116.md`](docs/OIC_Tech_Spec_Item_Import_Template_Specification_20260116.md) |
| サンプルファイルの内容を閲覧 | [`docs/Item_260306_0906_sample.md`](docs/Item_260306_0906_sample.md) |
| 一覧・再変換方法 | [`docs/README.md`](docs/README.md) |

再変換はプロジェクト直下で `python scripts/read_excel_to_md.py` を実行する（要 `pip install openpyxl`）。

開発時は上記2ファイル（Excel または Markdown）を開き、入力フォームの項目と出力シートの列の対応付け（マッピング）を定義すること。

---

## 次フェーズ

要件定義・ヒアリングの回答を反映済み。次のフェーズは**基本設計**とする。進め方と成果物の案は **[次のフェーズ_基本設計.md](次のフェーズ_基本設計.md)** を参照。

---

**以上**
